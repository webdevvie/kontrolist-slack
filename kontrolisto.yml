title: "slack-out"
version: "{{VERSION}}"
description: "Writes text to Slack channels"
type: "shell"
receives:
  - stdin
channels:
  - output
arguments:
  channel:
    required: false
    description: "The channel to write to"